# ESP32-S3 Partition Table — Dual OTA + Behavior Config
# Total flash: 16MB (adjust for your board)
# Usage: espflash flash --partition-table partitions.csv
#
# Layout:
#   nvs        — WiFi credentials, auth token, device settings
#   otadata    — OTA state (which app slot is active)
#   app0       — Factory/OTA firmware slot 0
#   app1       — OTA firmware slot 1
#   behavior   — Runtime-updatable component configs (survives firmware OTA)
#   spiffs     — General-purpose storage (logs, assets)
#
# To update only behavior configs post-flash:
#   1. Send new config JSON over WebSocket (command target="system", action="update_behavior")
#   2. Or use: espflash write-bin 0x310000 behavior.bin
#   3. Device reads behavior partition on boot and applies component configs

nvs,      data, nvs,     0x9000,  0x6000,
otadata,  data, ota,     0xf000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x1E0000,
app1,     app,  ota_1,   0x1F0000,0x1E0000,
behavior, data, spiffs,  0x3D0000,0x20000,
spiffs,   data, spiffs,  0x3F0000,0x10000,
